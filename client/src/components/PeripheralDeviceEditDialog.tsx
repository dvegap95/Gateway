import React from 'react'
import { PeripheralDevice } from '../entities/entities'

export default function PeripheralDeviceEditDialog(props:{device:PeripheralDevice}) {
  return (
    <div>PeripheralDeviceEditDialog</div>
  )
}
